# NimbusKart Cost Janitor — pinned dependencies
# Pin exact versions so GitHub Actions always gets the same environment.
# Update pins intentionally — never let pip choose versions automatically.

# AWS SDK
boto3==1.34.69
botocore==1.34.69

# AWS mocking for tests (no LocalStack needed in unit tests)
moto[ec2]==5.0.3

# Testing
pytest==8.1.1
pytest-cov==5.0.0

# Utilities
python-dateutil==2.9.0